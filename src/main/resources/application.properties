spring.application.name=DoorDrop

# ── Active Profile ─────────────────────────────────────────────────────────────
# Override with: -Dspring.profiles.active=prod  (or set env var SPRING_PROFILES_ACTIVE=prod)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}